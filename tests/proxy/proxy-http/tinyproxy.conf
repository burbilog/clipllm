# Minimal forward HTTP proxy config for proxy integration tests.
User nobody
Group nogroup
Port 8888
Listen 0.0.0.0
Timeout 600

# Allow any client (tests connect from the host via the published port).
Allow 0.0.0.0/0

# Log connection lines so `docker logs` is useful.
SysLog Off
LogLevel Connect
